# fromPairs

Takes an array of Tuple(s) (i.e pairs array) and returns an object composed of key-value pairs.

## Example

```javascript
_.fromPairs([
  ["a", 1],
  ["b", 2],
]);
// => { 'a': 1, 'b': 2 }
```
